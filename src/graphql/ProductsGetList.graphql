query ProductsGetList($take: Int!, $skip: Int!) {
	products(take: $take, skip: $skip) {
		data {
			id
			name
			categories {
				name
			}
			description
			rating
			images {
				url
				alt
			}
			price
		}
		meta {
			total
		}
	}
}
